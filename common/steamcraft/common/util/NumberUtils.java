package common.steamcraft.common.util;

public class NumberUtils {
int nothing = 0;
int theAnswer = 42;
int crash = -1;

public void doComplexMath(int input)
{
	input *= Math.E * Math.IEEEremainder(nothing, crash) + Math.sin(Math.PI * Math.tan(theAnswer));
}
}
